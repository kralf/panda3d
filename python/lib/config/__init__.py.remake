import os

PANDA_LIBRARY_PATH = "${LIBRARY_DESTINATION}"
PANDA_FILE_PATH = "${FILE_DESTINATION}"
PANDA_LIBRARY_PREFIX = "${PANDA3D_LIBRARY_PREFIX}"

PANDA_MODEL_PATH = os.path.join(PANDA_FILE_PATH, "models")
PANDA_SHADER_PATH = os.path.join(PANDA_FILE_PATH, "shaders")
